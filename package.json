{
  "name": "expo-unity-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo combining Expo Bare app, Unity integration, and web loader",
  "scripts": {
    "lint": "npm run lint -w app && npm run lint -w web",
    "typecheck": "npm run typecheck -w app && npm run typecheck -w web",
    "build:web": "npm run build -w web",
    "check:android": "node scripts/ensure-android-gradle.mjs"
  },
  "workspaces": [
    "app",
    "web",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
